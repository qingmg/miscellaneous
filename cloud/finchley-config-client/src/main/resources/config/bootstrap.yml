server:
  port: 10006
spring:
  application:
    name: config-client
  cloud:
    config:
      # 配置服务中心
      uri: http://192.168.1.123:10005/
      # 仓库分支
      label: master
      # dev  开发环境
      # test 测试环境
      # pro  正式环境
      profile: dev